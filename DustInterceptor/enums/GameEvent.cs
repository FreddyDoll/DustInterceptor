namespace DustInterceptor
{
    /// <summary>
    /// Identifies each unique game event in the random event system.
    /// </summary>
    public enum GameEvent
    {
        Intro,
        Iron10,
        OneMinute
    }
}
