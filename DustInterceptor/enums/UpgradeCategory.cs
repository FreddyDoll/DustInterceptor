namespace DustInterceptor
{
    /// <summary>
    /// Categories for grouping upgrades in the UI.
    /// </summary>
    public enum UpgradeCategory
    {
        Propulsion,
        Cryo,
        Structural
    }
}
